node_modules/
dist/
# Root manifest is a build artifact generated by packages/adapter-figma/build.plugin.mjs
# from manifest.base.json. Run `npm run build:plugin` to create it before importing
# the plugin into Figma. Not tracked because its JSON formatting (generator output vs
# biome canonical form) drifts between builds and pre-commit runs.
/manifest.json
*.js.map
*.DS_Store
.DS_Store
.env
.env.local
.claude/skills
.claude/settings.local.json
.kiro/skills
.kiro/settings/mcp.json
.vscode/
reports/benchmarks/
coverage/
.tmp/
Vibma-main/
